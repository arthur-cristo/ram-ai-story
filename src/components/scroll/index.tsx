import { SmoothScroll } from "./SmoothScroll";
import { ScrollProgress } from "./ScrollProgress";

const Scroll = () => (
  <>
    <SmoothScroll />
    <ScrollProgress />
  </>
);

export default Scroll;
